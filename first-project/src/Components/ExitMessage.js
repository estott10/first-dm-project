import React from 'react';

const ExitMessage = () =>{
    const checkOutLink= () => {
    };

    return(
        <div> {checkOutLink()}
        "Click the Link Above or the Full Story"
        </div>
    ) 
}

export default ExitMessage;