import React from 'react';

const Date = (props) => {
    return (
    <div>
      <p>Is {props.name} is bringing a plus one to the event? {props.plusOne}</p>   
      <p></p>
      <p></p>
    </div>
    )}

export default Date;