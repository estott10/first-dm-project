import React from 'react';

const MealSelection = (props) => {
    return (
    <div>
      <p>{props.name} would like to have {props.selection} for dinner.</p>   
      <p></p>
      <p></p>
    </div>
    )}

export default MealSelection;